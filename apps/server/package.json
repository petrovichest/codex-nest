{
  "name": "@codexnest/server",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup src/index.ts --format esm --platform node --target node24 --sourcemap --out-dir dist --clean",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:integration": "vitest run src/codex/integration.test.ts",
    "auth:generate": "tsx scripts/auth-generate.ts",
    "protocol:generate": "node scripts/generate-protocol.mjs"
  },
  "dependencies": {
    "@codexnest/protocol": "*",
    "@fastify/cors": "^11.1.0",
    "@fastify/static": "^10.1.0",
    "@fastify/websocket": "^11.2.0",
    "fastify": "^5.6.1",
    "firebase-admin": "^14.2.0",
    "undici": "^8.8.0",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "@types/ws": "^8.18.1",
    "tsup": "^8.5.0",
    "tsx": "^4.20.6",
    "vitest": "^3.2.4"
  }
}
